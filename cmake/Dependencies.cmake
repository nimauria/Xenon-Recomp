# External dependencies are intentionally minimal at the current hardware
# foundation milestone. Vulkan dependencies will be added with the Vulkan
# backend rather than leaking into the Xenos frontend target.
